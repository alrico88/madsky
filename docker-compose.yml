services:
  madsky:
    build:
      context: .
      dockerfile: Dockerfile
    image: madsky
    container_name: madsky
    environment:
      NODE_ENV: production
    restart: unless-stopped
    networks:
      - npm
networks:
  npm:
    external: true
